version: '3.1'

services:
  mongo:
    build: .
    ports:
      - 8080:8080
    restart: always
    volumes:
      - .:/app/back:z
      - /app/back/node_modules
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
